// print info in console:
// console.log('hello world')

// variable definieren mit dem Namen s und lege in die Variable hello:
let s = 'hello';
s = 1;

// Konstante create, Unterschied von Variable: wird einmalig definiert, 
const myConst = 111;
// darf nicht geändert werden, Fehler wird kommen:
// myConst = 2;

// mit var kann man auch, aber darf man besser nicht !!!
var myVariable = 333;

console.log(s);
console.log(myConst);